doctype html
head
 meta(charset='utf-8')
 title Recipe review
 link(rel='stylesheet' href='http://127.0.0.1:5000/Frontend-files/myStyle.css')

each val, index in reviews
 h2 #{reviews[index].username}
 h4 #{reviews[index].rating} / 5
 p #{reviews[index].reviewText}
